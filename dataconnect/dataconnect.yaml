specVersion: "v1beta"
serviceId: "showly"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "showly-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
